import React from 'react';
import ReactDOM from 'react-dom/client';
import MelbourneMayoralCalculator from './melbourne-mayoral-calculator';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MelbourneMayoralCalculator />
  </React.StrictMode>
);
